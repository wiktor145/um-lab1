Program should be run with python 3.8+.

To run, execute imagePreparer.py without any arguments.

If there is no file "dataset.ds", all images should be in proper directories in "files" directory.
If there is one, all data will be loaded from it.
